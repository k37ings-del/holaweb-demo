/**
 * Providers barrel. Compose app-wide providers in one place.
 */
export { AuthProvider, useAuth } from "@/contexts/AuthContext";
export { BusinessProvider, useBusiness } from "@/contexts/BusinessContext";
